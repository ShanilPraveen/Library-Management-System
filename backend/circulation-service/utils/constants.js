const BorrowingPeriod = 14;
const RenewalPeriod = 14;
const MaxActiveBorrowings = 2;
const penaltyPerDay = 2;
const defaultZero = 0;
const miliSecondsInDay = 1000 * 60 * 60 * 24;

module.exports = {
  BorrowingPeriod,
  RenewalPeriod,
  MaxActiveBorrowings,
  penaltyPerDay,
  defaultZero,
  miliSecondsInDay
};