const NAVBAR_HEIGHT = 64;
const SIDEBAR_WIDTH = 240;
const sliceStartIndex = 0;
const sliceEndIndex = 3;
const milisecondsInADay = 1000 * 60 * 60 * 24;
const maxDifDays = 7;
const minDifDays = 0;
const defaultZero = 0;
const remDaysUpperLimit = 7;
const remDaysMidLimit = 3;
const remDaysLowerLimit = 0;

export {
  NAVBAR_HEIGHT,
  SIDEBAR_WIDTH,
  sliceStartIndex,
  sliceEndIndex,
  milisecondsInADay,
  maxDifDays,
  minDifDays,
  defaultZero,
  remDaysUpperLimit,
  remDaysMidLimit,
  remDaysLowerLimit,
};
